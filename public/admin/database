-- CREATE DATABASE asd_techcreation;

-- CREATE TABLE clients (
--     id INT AUTO_INCREMENT PRIMARY KEY,
--     client_name VARCHAR(150) NOT NULL,
--     company_name VARCHAR(150),
--     email VARCHAR(150),
--     phone VARCHAR(30),
--     address TEXT,
--     gst_number VARCHAR(50),
--     project_name VARCHAR(150),
--     service VARCHAR(150),
--     project_value DECIMAL(12,2),
--     status ENUM('Active', 'Completed', 'On Hold') DEFAULT 'Active',
--     notes TEXT,
--     created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
-- );


-- CREATE TABLE invoices (

--     id INT AUTO_INCREMENT PRIMARY KEY,

--     client_id INT NOT NULL,

--     invoice_number VARCHAR(50) UNIQUE NOT NULL,

--     invoice_date DATE NOT NULL,

--     due_date DATE NOT NULL,

--     subtotal DECIMAL(12,2) DEFAULT 0,

--     gst_percent DECIMAL(5,2) DEFAULT 0,

--     gst_amount DECIMAL(12,2) DEFAULT 0,

--     discount DECIMAL(12,2) DEFAULT 0,

--     total_amount DECIMAL(12,2) DEFAULT 0,

--     paid_amount DECIMAL(12,2) DEFAULT 0,

--     balance_amount DECIMAL(12,2) DEFAULT 0,

--     status ENUM('Pending','Partial','Paid') DEFAULT 'Pending',

--     notes TEXT,

--     created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,

--     FOREIGN KEY (client_id)
--     REFERENCES clients(id)
--     ON DELETE CASCADE

-- );


-- CREATE TABLE invoice_items (

--     id INT AUTO_INCREMENT PRIMARY KEY,

--     invoice_id INT NOT NULL,

--     item_name VARCHAR(200) NOT NULL,

--     description TEXT,

--     quantity INT DEFAULT 1,

--     unit_price DECIMAL(12,2) DEFAULT 0,

--     total DECIMAL(12,2) DEFAULT 0,

--     FOREIGN KEY (invoice_id)
--     REFERENCES invoices(id)
--     ON DELETE CASCADE

-- );


-- CREATE TABLE payments (

--     id INT AUTO_INCREMENT PRIMARY KEY,

--     client_id INT NOT NULL,

--     invoice_id INT NOT NULL,

--     receipt_number VARCHAR(50),

--     payment_date DATE NOT NULL,

--     amount_paid DECIMAL(12,2) NOT NULL,

--     payment_method ENUM(
--         'Cash',
--         'UPI',
--         'Bank Transfer',
--         'Card'
--     ) DEFAULT 'UPI',

--     transaction_id VARCHAR(150),

--     notes TEXT,

--     created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,

--     FOREIGN KEY (client_id)
--     REFERENCES clients(id)
--     ON DELETE CASCADE,

--     FOREIGN KEY (invoice_id)
--     REFERENCES invoices(id)
--     ON DELETE CASCADE

-- );


-- ALTER TABLE invoices
-- ADD COLUMN signatory VARCHAR(50) DEFAULT 'Person 1';




-- ALTER TABLE invoices
-- ADD COLUMN signatory VARCHAR(50) DEFAULT 'Person 1';


-- CREATE TABLE company_profile (

-- id INT AUTO_INCREMENT PRIMARY KEY,

-- company_name VARCHAR(150),
-- logo VARCHAR(255),

-- owner_name VARCHAR(100),

-- email VARCHAR(150),
-- phone VARCHAR(30),

-- address TEXT,
-- city VARCHAR(100),
-- state VARCHAR(100),
-- pincode VARCHAR(20),

-- gst_number VARCHAR(50),
-- pan_number VARCHAR(50),

-- website VARCHAR(150),

-- bank_name VARCHAR(100),
-- account_holder VARCHAR(100),
-- account_number VARCHAR(50),
-- ifsc_code VARCHAR(50),
-- upi_id VARCHAR(100),

-- invoice_prefix VARCHAR(20) DEFAULT 'INV',
-- invoice_due_days INT DEFAULT 15,

-- signatory VARCHAR(100),

-- created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP

-- );


-- CREATE TABLE signatures (

--     id INT AUTO_INCREMENT PRIMARY KEY,

--     name VARCHAR(100) NOT NULL,

--     image VARCHAR(255) NOT NULL,

--     status ENUM('Active','Inactive') DEFAULT 'Active',

--     created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP

-- );


-- ALTER TABLE company_profile
-- DROP COLUMN signatory;


-- ALTER TABLE company_profile
-- ADD COLUMN signature_1_id INT,
-- ADD COLUMN signature_2_id INT;


-- ALTER TABLE invoices
-- DROP COLUMN signatory;

-- ALTER TABLE invoices
-- ADD COLUMN signature_id INT;

-- ALTER TABLE invoices

-- ADD CONSTRAINT fk_invoice_signature

-- FOREIGN KEY(signature_id)

-- REFERENCES signatures(id)

-- ON DELETE SET NULL;